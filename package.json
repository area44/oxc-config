{
  "name": "@area44/oxc-config",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build && pnpm -r run fmt",
    "lint": "pnpm -r run lint",
    "fmt": "pnpm -r run fmt",
    "version-packages": "changeset version",
    "release": "changeset publish"
  },
  "dependencies": {
    "oxfmt": "^0.67.0",
    "oxlint": "^1.82.0"
  },
  "devDependencies": {
    "@changesets/cli": "^3.0.2",
    "@types/node": "^25.9.6",
    "rolldown": "^1.2.8",
    "typescript": "^7.0.2"
  },
  "engines": {
    "node": ">=24.21.0"
  },
  "packageManager": "pnpm@12.4.1"
}
