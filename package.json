{
  "name": "oxc-config",
  "private": true,
  "packageManager": "pnpm@11.0.3",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint": "pnpm -r run lint",
    "format": "pnpm -r run format"
  },
  "dependencies": {
    "oxfmt": "^0.47.0",
    "oxlint": "^1.62.0"
  }
}
